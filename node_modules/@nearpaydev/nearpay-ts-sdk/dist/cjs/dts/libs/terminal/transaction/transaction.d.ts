import { JOB_COMMAND } from "../../jobs/types";
import { RemoteJobResponse } from "../../payload/types";
import { TransactionReceipt } from "../../models/transaction/transaction.types";
export declare class Transaction {
    private response;
    constructor(response: RemoteJobResponse<any>);
    getCommand(): JOB_COMMAND;
    getStatus(): any;
    getRecipts(): (TransactionReceipt | null)[];
    serialize(): RemoteJobResponse<any>;
    static Parse(json: RemoteJobResponse<any>): Transaction;
    isApproved(): boolean;
    /**
     * returns a print of the transaction
     */
    print(): void;
}
