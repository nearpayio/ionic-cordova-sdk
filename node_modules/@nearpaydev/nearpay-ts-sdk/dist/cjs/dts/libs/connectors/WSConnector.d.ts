import { AbstractWSConnector } from "./AbstractWSConnector";
import { NEARPAY_CONNECTOR } from "./types";
/**
 * Websocket Connector:
 *
 * this connector will be responsable for connecting to a
 * local device in the network using ip and port
 *
 * note: the websocket connection when creating a new WebSocket
 * object will not be async meaning that we should adabt a way
 * to make it connect in an async way.
 */
export declare class WsConnector extends AbstractWSConnector {
    static connector_name: string;
    private wsInfo;
    getType(): NEARPAY_CONNECTOR;
    static isAvaiale(): Promise<boolean>;
    getName(): string;
    getIp(): string;
    getPort(): string;
    /**
     * regular connection to ws service using async websocket connection
     * see asyncWebSocket class for more details
     */
    connect(profileId: string): Promise<void>;
}
