import { RemoteReconciliationResponse } from "../../payload/types";
export declare class Reconciliation {
    private response;
    constructor(response: RemoteReconciliationResponse);
    getStatus(): import("../../payload/types").RECONCILIATION_STATUS;
    getRecipt(): any;
    serialize(): RemoteReconciliationResponse;
    static Parse(json: RemoteReconciliationResponse): Reconciliation;
    isApproved(): boolean;
}
